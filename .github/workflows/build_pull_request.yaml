name: "Build Pull Request"
on: pull_request

permissions:
    contents: read

concurrency:
    group: build-pr-${{ github.event.pull_request.number }}
    cancel-in-progress: true

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v5
              env:
                  GIT_CONFIG_COUNT: 1
                  GIT_CONFIG_KEY_0: init.defaultBranch
                  GIT_CONFIG_VALUE_0: main
            - uses: actions/setup-java@v5
              with:
                  distribution: temurin
                  java-version: 17

            - uses: gradle/actions/setup-gradle@v5
            - name: Run unit tests
              run: sh ./gradlew testGithubDebugUnitTest --build-cache

            # Product flavors share all Kotlin/Java sources; the GitHub variant is the PR artifact.
            - name: Lint and assemble GitHub debug
              run: >-
                  sh ./gradlew
                  lintGithubDebug
                  assembleGithubDebug
                  --build-cache

            - uses: actions/upload-artifact@v5
              with:
                  name: github-${{ github.sha }}
                  path: app/build/outputs/apk/github/debug/*.apk
                  if-no-files-found: error
